# 阶段 2 首次安装 MVP 验收报告

验收日期：2026-07-16

## 完成范围

| 验收项 | 状态 | 说明 |
|---|---|---|
| 首次安装状态机 | 完成 | 检测、清理、下载、验证、插件安装、正式启用和状态保存 |
| 遗留暂存恢复 | 完成 | 新安装前清理上次未完成的暂存文件，不触碰正式 Vault |
| 多插件安装 | 完成 | 顺序安装锁定清单中的全部插件，逐文件执行 SHA-256 校验 |
| 社区插件确认 | 完成 | 安装前明确提示将执行社区插件代码，拒绝时不下载或写入 |
| 正式目录启用 | 完成 | 已验证 package 通过同卷目录移动启用到 `vault/{vault-id}` |
| 状态持久化 | 完成 | 保存 Vault ID、版本、路径、信任状态和最近成功时间 |
| 失败撤回 | 完成 | 正式目录移动后若设置保存失败，会移回暂存并清理半成品 |
| Obsidian 引导 | 完成 | 检测最低版本，支持用户确认后调用 WinGet或打开官方下载页 |
| 首次 Vault 注册 | 完成 | 首次安装后用绝对入口路径调用 Obsidian 并提示用户确认 |
| 后续打开 | 完成 | 根据正式目录名和清单入口构造 Obsidian URI |

## 自动化验证

- 7 个单元测试通过；
- 3 个集成测试通过；
- 覆盖知识库准备、首次正式安装、安装记录和设置保存失败撤回；
- Release 构建无警告、无错误；
- 代码格式验证通过。

## 阶段边界

本阶段只处理首次安装。已有知识库的版本检查、内容合并、备份、更新回滚和修复安装属于阶段 3，尚未在本阶段启用。

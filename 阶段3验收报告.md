# 阶段 3 更新、回滚与修复验收报告

验收日期：2026-07-16

## 完成范围

| 验收项 | 状态 | 说明 |
|---|---|---|
| 版本检查 | 完成 | 下载最新外部清单，比较已安装版本与最新版本 |
| 离线可用边界 | 完成 | 更新检查失败不影响已有 Vault 的“打开知识库”操作 |
| Preserve 策略 | 完成 | 按 glob 保留工作区、快捷键和插件个人配置 |
| Merge 策略 | 完成 | JSON 对象递归合并，上游提供默认字段，本地字段优先 |
| 策略冲突 | 完成 | 同一文件同时命中 preserve 和 merge 时停止更新 |
| Obsidian 占用检查 | 完成 | 检测到 Obsidian 进程时拒绝更新和修复 |
| 单版本备份 | 完成 | 正式切换前保存一个上一个可用版本 |
| 目录级切换 | 完成 | 当前 Vault 移至 backup，新 Vault 从 staging 移入正式路径 |
| 失败回滚 | 完成 | 新版本启用或设置保存失败时恢复原 Vault |
| 修复安装 | 完成 | 重新下载、验证和安装受管理内容，同时应用本地状态策略 |

## 自动化验证

- 13 个单元测试通过；
- 4 个集成测试通过；
- 覆盖 glob、JSON 合并、首次安装、备份切换与本地状态保留；
- Release 构建和格式检查通过。

## 阶段边界

当前采用保守占用策略：只要检测到 Obsidian 进程就停止更新，即使它可能正在打开另一个 Vault。后续可通过进程命令行、文件锁或 Obsidian 插件通信缩小检测范围。
